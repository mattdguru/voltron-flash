require 'spec_helper'

describe Voltron::Flash do
  it 'has a version number' do
    expect(Voltron::Flash::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
